<div class="container blog-container">
	<div class="row">
		<div class="blog-posts">
			<!--<h4 class="blog-count">Showing {{ data.posts.results.length}} post.</h4>-->
			<div class="blog-posts-container">
				{{# each data.posts.results}}
				<div data-ks-editable="{{#if ../user}}{{adminEditableUrl ../../user _id}}{{/if}}" class="blog-post">
					<!--<p class="lead text-muted">{{{categoryList categories prefix="Posted in "}}}-->
							<!--{{#if author.name.first}}by {{author.name.first}}{{/if}}-->
					<!--</p>-->
					{{#if image}}<a href="{{postUrl slug}}"><img src="{{{cloudinaryUrl image width=1280 height=720 crop='fill' quality="55" }}}" class="blog-img"></a>{{/if}}
					<h2 class="blog-title" ><a class="blog-title-link" href="{{postUrl slug}}">{{{title}}}</a></h2>
					<p class="blog-desc">{{{content.brief}}}</p>
					{{#if content.extended}}<p class="read-more"><a href="{{postUrl slug}}">Read more...</a></p>{{/if}}
				</div>
				{{/each}}
			</div>
		</div>
		<div class="blog-categories">
			<h2>Categories</h2>
			<div class="list-group">
				<a href="/blog" class="active list-group-item">All Categories</a>
				{{# each data.categories }}
				<a href="{{categoryUrl key}}" class="false list-group-item">{{name}}</a>
				{{/each}}
			</div>
		</div>
	</div>
	<div class="blog-pagination">
		{{>pagination}}
	</div>
</div>
no
